# 블로그 배포 아키텍처

## 📋 개요

이 문서는 AI 기반 투자 인사이트 블로그의 전체 배포 파이프라인 아키텍처를 설명합니다.

## 🏗️ 시스템 구성도

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          gemini-cli-git                                  │
│  (소스 저장소 - 에이전트 파이프라인)                                         │
│                                                                          │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐               │
│  │Market Watcher│───▶│Theme Analyst │───▶│Alt Perspect. │               │
│  │(gemini-flash)│    │(gemini-flash)│    │(gemini-flash)│               │
│  └──────────────┘    └──────────────┘    └──────────────┘               │
│                                                  │                       │
│                               ┌──────────────────┘                       │
│                               ▼                                          │
│                      ┌──────────────┐    ┌──────────────┐               │
│                      │Synth. Expert │───▶│Hugo Formatter│               │
│                      │(gemini-pro)  │    │(gemini-pro)  │               │
│                      └──────────────┘    └──────────────┘               │
│                                                  │                       │
│                                                  ▼                       │
│                      memory/skills/hugo_formatter/output/*.md            │
│                                                  │                       │
└──────────────────────────────────────────────────│───────────────────────┘
                                                   │
                                     sync-blog-deploy.yml
                                                   │
                                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     investment-insights-blog                             │
│  (배포 저장소 - Hugo 사이트)                                               │
│                                                                          │
│  content/posts/*.md  ──────▶  Hugo Build  ──────▶  public/              │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
                                                   │
                                           Git Push (main)
                                                   │
                                                   ▼
                                    ┌──────────────────────┐
                                    │       Vercel         │
                                    │  (자동 빌드 & 배포)    │
                                    └──────────────────────┘
                                                   │
                                                   ▼
                            https://investment-insights-blog.vercel.app
```

## 🔄 데이터 흐름

| 단계 | 위치 | 설명 |
|-----|------|------|
| 1 | gemini-cli-git | 매시간 `blog-pipeline.yml` 실행 |
| 2 | gemini-cli-git | 5단계 에이전트가 순차적으로 마켓 분석 수행 |
| 3 | gemini-cli-git | `hugo_formatter/output/`에 Hugo 포맷 `.md` 생성 |
| 4 | gemini-cli-git | PR 생성 → Editor 리뷰 → 자동 머지 |
| 5 | gemini-cli-git | `sync-blog-deploy.yml`이 변경된 포스트 감지 |
| 6 | investment-insights-blog | 변경된 `.md` 파일이 `content/posts/`로 복사 |
| 7 | Vercel | Git push 감지 후 Hugo 빌드 → CDN 배포 |

## 🔗 저장소 연결

### 필요한 Secrets (gemini-cli-git)

| Name | 용도 |
|------|------|
| `BLOG_DEPLOY_TOKEN` | GitHub PAT - investment-insights-blog 쓰기 권한 |
| `BLOG_DEPLOY_REPO` (Variable) | `adalgu/investment-insights-blog` |

### Vercel 설정

| 항목 | 값 |
|-----|-----|
| Framework | Hugo |
| Build Command | `hugo --minify` |
| Output Directory | `public` |
| Environment Variable | `HUGO_VERSION=0.128.0` |
